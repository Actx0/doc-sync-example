# Product overview

Acme Platform is a sample product used by this example knowledge base.

## Services

- **API** — public HTTP API for accounts and billing
- **Worker** — background jobs for invoices and notifications
- **Console** — operator dashboard

## Environments

| Name | Purpose |
| --- | --- |
| `dev` | local and preview |
| `staging` | pre-production |
| `prod` | customer traffic |

Keep this file in sync with the real architecture so agents and operators search the same source of truth.
